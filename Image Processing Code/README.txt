Use the unetCode to:
 -Train the segmentation network with the corresponding dataset.
 -Extract the generated mask with the dataset.

Use the imageProcessing to:
 -Do histogram equalizaton normalization to image
 -Do Gamma Correction normalization to image

The other files relate to above two code.